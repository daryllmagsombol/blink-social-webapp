sonar.projectKey=blink-social-media
sonar.projectName=Blink Social Media

# pnpm monorepo: analyze all app + package sources
sonar.sources=apps,packages

# Generated, vendored, and runtime content
sonar.exclusions=**/node_modules/**,**/.next/**,**/.turbo/**,**/dist/**,**/build/**,**/coverage/**,**/.git/**,**/.env*,**/*.test.*,**/*.spec.*,**/*.d.ts,docs/**,images/**,uploads/**,deploy/**

# No test runner configured yet — uncomment once Jest/Vitest + coverage exist:
# sonar.javascript.lcov.reportpaths=coverage/lcov.info
